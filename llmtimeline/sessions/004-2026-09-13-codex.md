# Session 004 · 2026-09-13 · Codex

## Upload revised Lemma policy
status: done
intent: publish the locally validated Warrant policy after the user explicitly authorized sending it to the configured Lemma project.
did: uploaded the 6,275-character policy and behavior-contract wrapper for warrant.investigate, warrant.execute, warrant.decline and warrant.replay; read the artifact listing back and confirmed every agent registration. Lemma also returned its generated 8,744-character understanding document.
files: llmtimeline/state.md, llmtimeline/sessions/004-2026-09-13-codex.md
decisions: this action was limited to the Lemma policy upload; no Stripe, GitHub, Slack, Slack failure scenario or Arga invocation ran.
gotchas: warrant.decline and warrant.replay were listed with a null latest-version field immediately after upload, but both had successful upload IDs and were present in the verification listing.
next: review the policy under Lemma Artifacts; leave issue triage and provider-writing verification as separately authorized work.

## Run focused S1 end to end
status: done
intent: exercise only the genuine duplicate-refund scenario without running S2, replay, S6, the Slack failure scenario or Arga.
did: added a scenario selector to the E2E harness, built a fresh S1 Stripe/GitHub fixture, and ran it with real Slack plus an isolated temporary SQLite journal. The model proposed plan wpl_da6bc0fa9249f092f658b38ec81340924ec6d97faa5d34a864ae4d497b91a1ea; scripted human approval resumed execution; Stripe confirmed 4900 refunded on the later charge and zero refunded on the legitimate charge; GitHub case #161 and Slack updates completed.
files: verify/e2e.mjs, fixtures/current.json, llmtimeline/state.md, llmtimeline/sessions/004-2026-09-13-codex.md
decisions: expose `--scenario=s1` for focused rehearsals; isolate the test journal with `WARRANT_DB`; explicitly clear `SLACK_API_URL` so the run uses Slack directly and cannot reach Arga.
gotchas: fixture creation also created GitHub incident #160 and new Stripe test data. The approved refund is consumed, so rerunning S1 requires rebuilding its fixture.
next: use `node fixtures/build.mjs --scenario s1` before another S1 run; continue to keep S4/Arga opt-in.

## Function-based SQLite S1 demo backend
status: done
intent: support the customer complaint to admin-approved refund demo through callable backend functions and a repeatable SQLite mock, without React components or frontend edits.
did: added a SQLite provider for customers, charges, incidents, case files, notifications and refunds; added shared application functions for complaint submission, investigation, plan-bound approval, execution and decline; added S1 seed/reset/status functions; exposed demo reset/status and existing workflow functions through thin HTTP routes; made existing fixture/reset endpoints SQLite-aware; added separate demo commands/database and operations documentation. A direct test and an HTTP rehearsal both proved that investigation stops at approval with both charges untouched, stale plan approval fails, valid admin approval refunds only the later charge, and case/notification records finish.
files: src/application.js, src/demo-store.js, src/demo.js, src/adapters/stripe.js, src/adapters/github.js, src/adapters/slack.js, src/server.js, scripts/prepare-demo.mjs, test/demo.test.mjs, package.json, .env.example, docs/operations.md, llmtimeline/state.md, llmtimeline/sessions/004-2026-09-13-codex.md
decisions: use `WARRANT_PROVIDER=sqlite` as an adapter switch; keep the LangGraph, policy guard, approval hash, journal and persistence path identical; isolate demo state in `data/warrant-demo.db`; keep browser code free of provider credentials and script execution.
gotchas: SQLite demo mode intentionally records GitHub/Slack equivalents locally, so provider links should be treated as local records. The running verification server uses rules mode and a temporary database; normal `npm run start:demo` uses the configured LLM unless `LLM_MODE=rules` is set.
next: inspect the existing vanilla frontend against the backend at http://localhost:3100; frontend redesign remains a separate task.

## Restore sponsor integrations to the hackathon demo
status: done
intent: correct the earlier over-broad SQLite interpretation so the judged S1 path demonstrably uses at least Stripe, GitHub, Slack and Lemma.
did: made `start:demo` force the live provider; turned the S1 fixture builder into exported backend functions; added `prepareIntegratedS1Demo()` and a CLI wrapper; exposed integrated reset through the demo endpoint; isolated live-demo workflow state in `data/warrant-live-demo.db`; retained SQLite provider mode under explicit `:sqlite` commands for tests. Prepared a fresh Stripe test customer and duplicate pair plus GitHub incident #163 and started the integrated server with Lemma tracing enabled.
files: fixtures/build.mjs, src/demo-live.js, src/server.js, scripts/prepare-live-demo.mjs, package.json, docs/operations.md, llmtimeline/state.md, llmtimeline/sessions/004-2026-09-13-codex.md
decisions: SQLite is Warrant's durable coordination/audit database in the hackathon demo, not a replacement for sponsor applications. Leave the fresh S1 pair unrefunded until the interactive demo.
gotchas: an attempted reset against the normal database was automatically rejected due to audit-data loss risk; the dedicated live-demo database resolved this safely. The current live fixture is customer `cus_VFqGnk0610cOWl`, charges `ch_3UFKZrIa2TNAqGWf2395uoDR` and `ch_3UFKZsIa2TNAqGWf2b4XBJcu`, GitHub incident #163.
next: open http://localhost:3100, submit `billing@northwind.test`, then approve from /admin; this will call the configured model, Stripe, GitHub, Slack and Lemma.

## Clarify and correct demo reset semantics
status: done
intent: verify which S1 records were real and make the existing reset control repeat the integrated demo safely.
did: confirmed the running server was in live mode and read both S1 charges directly from Stripe; the kept charge had amount_refunded=0 and the later charge had amount_refunded=4900. Added an explicit WARRANT_DEMO=s1 mode so fixture responses expose only S1 and the existing reset endpoint invokes the integrated preparation function, creating a fresh Stripe test pair and GitHub incident while resetting only the isolated live-demo database.
files: src/server.js, package.json, .env.example, llmtimeline/sessions/004-2026-09-13-codex.md
decisions: the visible Reset Demo Data action represents a full integrated reset in hackathon demo mode; normal live mode retains its local-only reset behavior.
gotchas: each integrated reset intentionally creates new Stripe test objects and a new GitHub fixture incident; it does not delete earlier provider fixtures.
next: use Reset Demo Data before each new S1 take, wait for preparation to finish, then submit from the customer page.

## Main payment-first simulation
status: done
intent: move the payment cold open into main and make the refund action a persistent customer affordance.
did: added `src/simulation.js` and `POST /api/simulation/payment`, serving `/simulation` from the main vanilla server. The page creates two Stripe test PaymentIntents, renders actual returned charge IDs, and keeps “Multiple charge? Ask for a refund” visible at all times. The handoff links to the existing complaint form with the S1 customer email and report prefilled.
files: src/simulation.js, src/server.js, public/simulation.html, public/index.html, llmtimeline/state.md, llmtimeline/sessions/004-2026-09-13-codex.md
decisions: use the payment itself as the fresh S1 fixture; Reset Demo Data is optional for the first take and remains useful only for clearing local Warrant workflow state between runs. Do not invoke the payment endpoint during offline verification because it creates real Stripe test objects.
gotchas: each click on Pay creates a new Stripe test customer and two charges; the button is disabled while the request is active but can be used again after an error.
next: run the integrated demo from `/simulation` with the configured Stripe test key, then submit the prefilled complaint and approve from `/admin`.
