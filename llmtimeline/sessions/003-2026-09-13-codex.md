# Session 003 · 2026-09-13 · Codex

## Implement reliability and Lemma improvements
status: in_progress
intent: unify guards and rules mode, clarify outcomes, complete journal coverage, add Lemma visibility/holds and durable workflow recovery with offline regression coverage.
decisions: authentication excluded by user; no Slack failure scenario or Arga runs; no external Slack/GitHub messages during validation.
next: implement shared policy guard and persistent storage.

## Backend reliability and Lemma implementation
status: done
intent: implement the approved reliability sequence without frontend/dashboard changes or Slack failure/Arga execution.
did: unified model and deterministic findings under one duplicate guard; added explicit refund-state outcomes; honoured LLM_MODE and LLM_FALLBACK; journalled initial GitHub/Slack writes and scoped case follow-ups per run; fail-closed ambiguous writes with an audited reconciliation script; persisted runs and LangGraph checkpoints in SQLite; recovered paused, held, partial and completed runs; rechecked run-specific approval and both charges before new refunds; added full-message and approval tracing; added Lemma issue pagination, trace occurrences, backend APIs and warrant-hold enforcement; made S4 explicitly opt-in.
files: src/, scripts/, verify/e2e.mjs, test/, package.json, package-lock.json, .env.example, README.md, docs/project-understanding.md
decisions: no authentication work; no frontend/dashboard edits; use read-only Lemma polling because the current public schema exposes issue/trace reads but no documented webhook delivery contract; keep policy upload and issue triage as separate external mutations.
gotchas: existing local data migrates automatically. Ambiguous remote writes remain stopped until a person inspects the provider and reconciles them. SQLite design assumes one application process. Test module mocking emits Node experimental/deprecation warnings but all assertions pass.
next: review and optionally upload the revised Lemma policy; keep S4/Arga excluded unless explicitly requested.

## Multiple duplicate debit handling
status: done
intent: prevent a three-or-more charge incident from receiving a misleading one-refund correction.
did: added deterministic grouping for succeeded, unrefunded charges with matching amount/currency inside one 24-hour window and shared non-empty descriptions or incident corroboration. The finding records the likely legitimate charge and every excess charge, writes a manual-review case/notification, and builds no plan. Applied to both model and rules paths.
files: src/duplicate-policy.js, src/agent-rules.js, src/outcomes.js, src/graph.js, src/run.js, src/complaints.js, src/policy.md, test/backend.test.mjs, test/policy.test.mjs, README.md, docs/project-understanding.md
decisions: preserve one-refund-per-plan semantics; multiple refunds require a future explicit batch plan and batch approval rather than silently correcting only one charge.
gotchas: detection is limited to charges the investigation actually retrieved. Frontend files remain unchanged, so the richer outcome is currently visible through backend responses, case content and Slack content.
next: no further backend work required for this outcome; a future frontend task may render `manual_review` and `multiple_duplicates` explicitly.

## Safe batch refunds and guarded extended window
status: done
intent: correct every excess charge in one human-approved batch, while preserving restart, replay and uncertain-write safety; allow older incidents without treating monthly renewals as duplicates.
did: changed `multiple_duplicates` into an actionable batch finding and plan; bound ordered refund items, per-item amount/currency/duplicate target, kept charge and total into the plan hash; re-read all involved Stripe charges before any new write; executed sequential full refunds with `refund_full:<charge>` journal/idempotency keys; verified every charge readback; derived complete/partial/failed/uncertain aggregate states; made definitive failures restartable without repeating successful items and uncertain writes reconciliation-only; added batch GitHub/Slack proposal, applied and decline content; normalized strong Stripe invoice/order/checkout keys; parsed explicit `incident_start` and `incident_end` GitHub fields; allowed up to 30 days only when both strong shared identity and full incident coverage exist; exposed this evidence to the model and Lemma traces; updated local policy and documentation.
files: src/plan.js, src/duplicate-policy.js, src/graph.js, src/journal.js, src/adapters/stripe.js, src/adapters/github.js, src/adapters/slack.js, src/agent-tools.js, src/complaints.js, src/outcomes.js, src/policy.md, src/run.js, scripts/upload-policy.mjs, test/helpers.mjs, test/backend.test.mjs, test/policy.test.mjs, README.md, docs/project-understanding.md
decisions: keep 24 hours as the default; subscription identity alone is not a strong key because it would group legitimate renewals; require incident body fields `incident_start:` and `incident_end:` for extended grouping; retain successful refunds when a later item fails; never retry an ambiguous provider result without journal reconciliation.
gotchas: revised Lemma policy was printed and syntax-checked but not uploaded. Tests were offline and did not call real Stripe, GitHub, Slack, Arga or the Slack failure scenario. Frontend files were not changed.
next: provider-writing rehearsal and Lemma policy upload remain separate external actions; do not run S4/Arga or triage Lemma issues without the user's direction.

## Deep architecture review after batch implementation
status: done
intent: inspect the complete backend agent flow for correctness gaps, failure modes, recovery problems, and avoidable cost or latency without invoking external provider writes.
did: traced investigation/tool routing, deterministic policy, plan/approval binding, journal semantics, Stripe/GitHub/Slack adapters, LangGraph checkpoints, restart/replay, complaint projections, Lemma flow-back and tests. Confirmed 36/36 offline tests and clean diff checks. Found priority issues: Stripe refund calls omit the approved amount and journal success precedes semantic result validation; financial partial batches are represented to customers as fully resolved; pagination completeness is not part of eligibility; batch GitHub case text uses the single-plan fields and prints null IDs; partial execution cannot be reapproved after TTL; nearby incidents need no billing relevance; automatic batch count/value is uncapped. Also found fallback incident/search asymmetry, missing refund-item pending details, full-history scans in every sync, unbounded persisted history, 64-bit plan hashes, duplicate complaint status key, and local Lemma policy not uploaded.
files: llmtimeline/state.md, llmtimeline/sessions/003-2026-09-13-codex.md
decisions: review only; do not change application behavior until the user selects or authorizes the remediation pass. Keep authentication and frontend out of scope per prior direction.
gotchas: `npm audit --omit=dev` could not reach the npm registry in the restricted network, so dependency advisory status was not established. No Slack failure scenario, Arga, provider write, or Lemma upload ran.
next: implement the priority correctness fixes and focused tests before provider-writing rehearsal.

## Architecture review remediation
status: in_progress
intent: fix the correctness, recovery, evidence, audit and scaling issues found in the post-batch deep review, with offline regression tests and no frontend/provider-writing changes.
next: harden refund execution and financial state semantics first, then evidence completeness, reapproval, policy bounds and persistence queries.

## Architecture review remediation completed
status: done
intent: close the correctness and scaling gaps identified in the deep review without frontend or external-provider changes.
did: sent exact approved amounts to Stripe and required succeeded matching responses inside the journal boundary; verified charge identity/customer/amount readback; split financial_partial from notification_partial with truthful customer progress; added item-level refund pending state; allowed fresh approval for unfinished expired work; upgraded new plan hashes to full SHA-256 with legacy verification; required complete relevant charge pagination before batch proposals; anchored model batches to selected IDs and prevented inconclusive findings from being upgraded; required billing-relevant incidents and exact billing-key coverage for extended windows; capped batches at 10 refunds and 100000 minor units with execution-time defense; fixed batch GitHub audit text and backend batch totals; recorded cross-run operation references; bounded request bodies; added checkpoint indexes, SQL filtering and constant-time counts; broadened fallback incident dates; updated policy/Lemma context and documentation.
files: src/adapters/stripe.js, src/adapters/github.js, src/duplicate-policy.js, src/agent-rules.js, src/agent-tools.js, src/agent.js, src/graph.js, src/plan.js, src/journal.js, src/run.js, src/runstate.js, src/complaints.js, src/outcomes.js, src/checkpointer.js, src/storage.js, src/server.js, src/policy.md, scripts/upload-policy.mjs, test/helpers.mjs, test/backend.test.mjs, test/policy.test.mjs, README.md, docs/project-understanding.md
decisions: preserve global charge-level idempotency while recording per-run references; retain old 16-hex plan verification only for persisted plans and use full hashes for new plans; treat non-succeeded Stripe refund states and response mismatches as uncertain; keep production multi-process coordination and retention/archive policy as deployment architecture work rather than silently deleting financial audit history.
gotchas: revised Lemma policy was validated with `--print` but not uploaded. Tests are offline; no real Stripe/GitHub/Slack writes, Slack failure scenario or Arga run occurred. Node test mocks still emit experimental/deprecation warnings.
next: provider-writing rehearsal remains separate; for production deployment add shared coordination and a formal audit archive/retention policy.

## Production coordination and audit retention
status: in_progress
intent: add multi-process coordination for a shared SQLite deployment, preserve financial audit history while pruning replay-heavy state under an explicit policy, and publish the already validated Lemma contract.
decisions: retain Stripe/GitHub/Slack provider writes and Slack failure/Arga exclusion from the earlier user constraint; Lemma policy upload is the only planned external mutation.
next: implement renewable run leases, terminal timestamps and dry-run-first archive tooling, then test before uploading policy.

## Production coordination and audit retention completed
status: done
intent: make shared-SQLite multi-process execution safe and define a deliberate, non-destructive audit lifecycle.
did: added renewable 60-second run leases with crash expiry around every graph segment; added terminal closedAt timestamps; added dry-run-first `npm run history:archive -- --days=N [--apply]`; archive snapshots the run, complaints, approval and operations before pruning only checkpoint rows; documented the one-host SQLite boundary and multi-host shared-database requirement; added lease contention/expiry and archive preservation tests. Validated the full suite, syntax, graph generation, policy rendering and diff hygiene.
files: src/coordinator.js, src/retention.js, src/run.js, src/runstate.js, src/storage.js, scripts/archive-history.mjs, docs/operations.md, package.json, test/operations.test.mjs, llmtimeline/state.md, llmtimeline/sessions/003-2026-09-13-codex.md
decisions: financial journal and approval records are retained; routine archival removes only replay-heavy checkpoint data after first writing an audit snapshot. SQLite leases support multiple processes only when they share the same local database file.
gotchas: 46/46 offline tests pass. The Lemma upload failed in the sandbox on DNS, then automatic approval review rejected escalation because explicit authorization for sending the exact local policy payload to `api.uselemma.ai` was required. No policy upload, Stripe/GitHub/Slack write, Slack failure scenario or Arga run occurred.
next: obtain explicit authorization for the Lemma policy payload if upload is still desired; keep multi-host deployment on a shared transactional database.
